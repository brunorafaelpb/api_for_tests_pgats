{
  "name": "api-transferencias",
  "version": "1.0.0",
  "description": "API Rest para login, registro, consulta de usuários e transferências, com banco em memória.",
  "main": "server.js",
  "scripts": {
    "test": "mocha 'test/**/*.test.js' --timeout 10000",
    "test-api": "mocha 'test/api/**/*.test.js' --timeout 10000",
    "test-controller": "mocha 'test/controller/**/*.test.js' --timeout 10000",
    "start": "node server.js"
  },
  "dependencies": {
    "chai": "^4.5.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mocha": "^11.7.1",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4",
    "swagger-ui-express": "^4.6.3"
  }
}
